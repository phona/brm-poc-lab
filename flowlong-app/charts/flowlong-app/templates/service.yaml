apiVersion: v1
kind: Service
metadata:
  name: {{ include "flowlong-app.fullname" . }}
  labels:
    {{- include "flowlong-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flowlong-app.selectorLabels" . | nindent 4 }}
